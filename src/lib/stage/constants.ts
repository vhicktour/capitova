/** The invisible spacer whose height defines the timeline's scroll range. */
export const SCROLL_TRACK_ID = "scroll-track";

/** Set once the intro flight has played, so it plays once per session. */
export const INTRO_SEEN_KEY = "granule.intro";
